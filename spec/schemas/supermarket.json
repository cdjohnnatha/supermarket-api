{
  "allOf": [
    { "$ref": "core.json#/definitions/single_core/data" },
    {"attributes": 
      {
        "required": ["id", "name", "description"],
        "properties": {
          "id": { "type": "integer" },
          "name": { "type": "string" },
          "description": { "type": "string" }
        }
      }
    }
  ]
}